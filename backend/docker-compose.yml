version: '3.8'

services:
  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis:latest
    ports:
      - "6379:6379"

  jaeger:
    image: jaegertracing/all-in-one:latest
    ports:
      - "16686:16686" # UI
      - "14268:14268" # Collector
      - "4317:4317"  # OTLP gRPC
      - "4318:4318"  # OTLP HTTP

  gateway:
    build:
      context: .
      dockerfile: ./gateway/Dockerfile
    ports:
      - "5000:5000"
    environment:
      - AUTH_SERVICE_URL=http://auth:8000
      - BLOG_SERVICE_URL=http://blog:8001
      - INTERACTIONS_SERVICE_URL=http://interactions:8002
      - MEDIA_SERVICE_URL=http://media:8003
      - REALTIME_SERVICE_URL=http://realtime:8004
      - FEED_SERVICE_URL=http://feed:8005
      - SEARCH_SERVICE_URL=http://search:8006
      - CHAT_SERVICE_URL=http://chat:8007
      - JAEGER_ENDPOINT=http://jaeger:14268/api/traces
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
      - jaeger

  auth:
    build:
      context: .
      dockerfile: ./services/auth/Dockerfile
    environment:
      - PORT=8000
      - MONGODB_URI=mongodb://mongodb:27017/Zuvo
      - REDIS_URL=redis://redis:6379
      - JAEGER_ENDPOINT=http://jaeger:14268/api/traces
    depends_on:
      - mongodb
      - redis
      - jaeger

  blog:
    build:
      context: .
      dockerfile: ./services/blog/Dockerfile
    environment:
      - PORT=8001
      - MONGODB_URI=mongodb://mongodb:27017/Zuvo
      - REDIS_URL=redis://redis:6379
      - JAEGER_ENDPOINT=http://jaeger:14268/api/traces
      - AUTH_INTERNAL_URL=http://auth:8000
      - BLOG_INTERNAL_URL=http://blog:8001
      - INTERACTIONS_INTERNAL_URL=http://interactions:8002
    depends_on:
      - mongodb
      - redis
      - jaeger

  interactions:
    build:
      context: .
      dockerfile: ./services/interactions/Dockerfile
    environment:
      - PORT=8002
      - MONGODB_URI=mongodb://mongodb:27017/Zuvo
      - REDIS_URL=redis://redis:6379
      - JAEGER_ENDPOINT=http://jaeger:14268/api/traces
      - AUTH_INTERNAL_URL=http://auth:8000
      - BLOG_INTERNAL_URL=http://blog:8001
      - INTERACTIONS_INTERNAL_URL=http://interactions:8002
    depends_on:
      - mongodb
      - redis
      - jaeger

  media:
    build:
      context: .
      dockerfile: ./services/media/Dockerfile
    environment:
      - PORT=8003
      - MONGODB_URI=mongodb://mongodb:27017/Zuvo
      - REDIS_URL=redis://redis:6379
      - CLOUDINARY_CLOUD_NAME=${CLOUDINARY_CLOUD_NAME}
      - CLOUDINARY_API_KEY=${CLOUDINARY_API_KEY}
      - CLOUDINARY_API_SECRET=${CLOUDINARY_API_SECRET}
      - JAEGER_ENDPOINT=http://jaeger:14268/api/traces
    depends_on:
      - mongodb
      - redis
      - jaeger

  realtime:
    build:
      context: .
      dockerfile: ./services/realtime/Dockerfile
    environment:
      - PORT=8004
      - MONGODB_URI=mongodb://mongodb:27017/Zuvo
      - REDIS_URL=redis://redis:6379
      - JAEGER_ENDPOINT=http://jaeger:14268/api/traces
      - CORS_ORIGIN=http://localhost:3000
    depends_on:
      - mongodb
      - redis
      - jaeger

  worker:
    build:
      context: .
      dockerfile: ./services/worker/Dockerfile
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/Zuvo
      - REDIS_URL=redis://redis:6379
      - JAEGER_ENDPOINT=http://jaeger:14268/api/traces
      - AUTH_INTERNAL_URL=http://auth:8000
      - BLOG_INTERNAL_URL=http://blog:8001
      - INTERACTIONS_INTERNAL_URL=http://interactions:8002
    depends_on:
      - mongodb
      - redis
      - jaeger

  feed:
    build:
      context: .
      dockerfile: ./services/feed/Dockerfile
    environment:
      - PORT=8005
      - MONGODB_URI=mongodb://mongodb:27017/Zuvo
      - REDIS_URL=redis://redis:6379
      - JAEGER_ENDPOINT=http://jaeger:14268/api/traces
      - AUTH_INTERNAL_URL=http://auth:8000
      - BLOG_INTERNAL_URL=http://blog:8001
      - INTERACTIONS_INTERNAL_URL=http://interactions:8002
    depends_on:
      - mongodb
      - redis
      - jaeger

  search:
    build:
      context: .
      dockerfile: ./services/search/Dockerfile
    environment:
      - PORT=8006
      - MONGODB_URI=mongodb://mongodb:27017/Zuvo
      - REDIS_URL=redis://redis:6379
      - JAEGER_ENDPOINT=http://jaeger:14268/api/traces
      - AUTH_INTERNAL_URL=http://auth:8000
      - INTERACTIONS_INTERNAL_URL=http://interactions:8002
      - BLOG_INTERNAL_URL=http://blog:8001
    depends_on:
      - mongodb
      - redis
      - jaeger

  chat:
    build:
      context: .
      dockerfile: ./services/chat/Dockerfile
    environment:
      - PORT=8007
      - MONGODB_URI=mongodb://mongodb:27017/Zuvo
      - REDIS_URL=redis://redis:6379
      - JAEGER_ENDPOINT=http://jaeger:14268/api/traces
    depends_on:
      - mongodb
      - redis
      - jaeger

volumes:
  mongo-data:
